By the task's `phase`: `fix` means your program threw or misbehaved on a move; the task carries
the `error` and the record it failed on. Read the current `go-fish-dealer` workspace, fix
`dealer.js`, save the new version, and answer with one line on what changed. The failed move
retries by itself; hand nothing on. `write` means a new game: the program already exists, so keep
it unless the task says otherwise, and hand on {tags: ["dealer"], phase: "setup", players, ranks}.
