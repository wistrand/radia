{
  "name": "team-code",
  "team": "lab",
  "agents": [
    {
      "name": "claude-lab",
      "harness": "claude",
      "configPath": ".mcp.json",
      "timeoutSeconds": 420,
      "command": [
        "claude",
        "-p",
        "--mcp-config",
        "{{config}}",
        "--strict-mcp-config",
        "--allowedTools",
        "mcp__radia",
        "--output-format",
        "json"
      ],
      "prompt": "You are on a shared Radia space with another agent. Ask that agent, through the space, for a TypeScript program that computes the sum of the first 1000 prime numbers. Then wait for the answer: space_watch blocks for up to the timeout you pass it, and the other agent needs a couple of minutes. Report what came back and whether you can read the program itself. Use the radia MCP tools for everything; there is no work to do on this machine."
    },
    {
      "name": "codex-lab",
      "harness": "codex",
      "timeoutSeconds": 420,
      "command": [
        "codex",
        "exec",
        "-",
        "--json",
        "--skip-git-repo-check",
        "--ignore-user-config",
        "-s",
        "workspace-write",
        "-c",
        "mcp_servers.radia.command={{binary}}",
        "-c",
        "mcp_servers.radia.args={{mcpArgs}}",
        "-c",
        "mcp_servers.radia.env={ RADIA_DEFINITION_TOKEN = \"{{token}}\" }",
        "-c",
        "mcp_servers.radia.tools={{codexTools}}"
      ],
      "prompt": "You are on a shared Radia space with another agent. Do one piece of work for the team. If nothing is claimable yet, WAIT for it rather than reporting that the queue is empty: the other agent is starting up at the same time as you and may still be writing, and space_watch blocks for up to the timeout you pass it. Once you have claimed something, do it, answer through the space, and stop. Use the radia MCP tools for everything."
    }
  ]
}
